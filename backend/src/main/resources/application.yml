spring:
  datasource:
    url: jdbc:postgresql://localhost:5455/easydraw
    username: postgres
    password: psql24678
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  redis:
    host: localhost
    port: 6379
    password:
    database: 0
  websocket:
    broker:
      enabled: true

server:
  port: 8080

jwt:
  secret: easydraw_secret_key
  expiration: 86400000
